{{#>dialogueBubble}}
	{{#*inline 'title'}}
		{{translate 'dialogue.change-headline'}}
	{{/inline}}

	{{#*inline 'content'}}
		<form>
			<div class="form-group">
				<input type="text" name="change"><button class="green-btn" type="submit">{{translate 'dialogue.next'}}</button>
			</div>
		</form>
	{{/inline}}
{{/dialogueBubble}}
